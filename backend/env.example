# PostgreSQL connection string for the FastAPI service.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/vazhikal
SQL_ECHO=false
# Optional comma-separated browser origins. LAN addresses on port 3000 are allowed by default.
# CORS_ORIGINS=http://192.168.1.20:3000
